---
created: 2026-04-19T05:38:35 (UTC +03:00)
tags: []
source: https://en.wikipedia.org/wiki/DBSCAN
author: Contributors to Wikimedia projects
---

# DBSCAN - Wikipedia

> ## Excerpt
> From Wikipedia, the free encyclopedia

---
From Wikipedia, the free encyclopedia

**Density-based spatial clustering of applications with noise** (**DBSCAN**) is a [data clustering](https://en.wikipedia.org/wiki/Data_clustering "Data clustering") [algorithm](https://en.wikipedia.org/wiki/Algorithm "Algorithm") proposed by [Martin Ester](https://en.wikipedia.org/wiki/Martin_Ester "Martin Ester"), [Hans-Peter Kriegel](https://en.wikipedia.org/wiki/Hans-Peter_Kriegel "Hans-Peter Kriegel"), [Jörg Sander](https://en.wikipedia.org/w/index.php?title=J%C3%B6rg_Sander&action=edit&redlink=1 "Jörg Sander (page does not exist)"), and [Xiaowei Xu](https://en.wikipedia.org/w/index.php?title=Xiaowei_Xu&action=edit&redlink=1 "Xiaowei Xu (page does not exist)") in 1996.<sup id="cite_ref-dbscan_1-0"><a href="https://en.wikipedia.org/wiki/DBSCAN#cite_note-dbscan-1"><span>[</span>1<span>]</span></a></sup> It is a [density-based clustering](https://en.wikipedia.org/wiki/Cluster_analysis#Density-based_clustering "Cluster analysis") non-parametric algorithm: given a set of points in some space, it groups together points that are closely packed (points with many [nearby neighbors](https://en.wikipedia.org/wiki/Fixed-radius_near_neighbors "Fixed-radius near neighbors")), and marks as outliers points that lie alone in low-density regions (those whose nearest neighbors are too far away). DBSCAN is one of the most commonly used and cited clustering algorithms.<sup id="cite_ref-2"><a href="https://en.wikipedia.org/wiki/DBSCAN#cite_note-2"><span>[</span>2<span>]</span></a></sup>

In 2014, the algorithm was awarded the Test of Time Award (an award given to algorithms which have received substantial attention in theory and practice) at the leading data mining conference, ACM [SIGKDD](https://en.wikipedia.org/wiki/SIGKDD "SIGKDD").<sup id="cite_ref-3"><a href="https://en.wikipedia.org/wiki/DBSCAN#cite_note-3"><span>[</span>3<span>]</span></a></sup> As of July 2020, the follow-up paper "DBSCAN Revisited, Revisited: Why and How You Should (Still) Use DBSCAN"<sup id="cite_ref-tods_4-0"><a href="https://en.wikipedia.org/wiki/DBSCAN#cite_note-tods-4"><span>[</span>4<span>]</span></a></sup> appears in the list of the 8 most downloaded articles of the prestigious [ACM Transactions on Database Systems (TODS)](https://en.wikipedia.org/wiki/ACM_Transactions_on_Database_Systems "ACM Transactions on Database Systems") journal.<sup id="cite_ref-5"><a href="https://en.wikipedia.org/wiki/DBSCAN#cite_note-5"><span>[</span>5<span>]</span></a></sup>

Another follow-up, **HDBSCAN\***, was initially published by Ricardo J. G. Campello, David Moulavi, and Jörg Sander in 2013,<sup id="cite_ref-hdbscan1_6-0"><a href="https://en.wikipedia.org/wiki/DBSCAN#cite_note-hdbscan1-6"><span>[</span>6<span>]</span></a></sup> then expanded upon with [Arthur Zimek](https://en.wikipedia.org/wiki/Arthur_Zimek "Arthur Zimek") in 2015.<sup id="cite_ref-hdbscan2_7-0"><a href="https://en.wikipedia.org/wiki/DBSCAN#cite_note-hdbscan2-7"><span>[</span>7<span>]</span></a></sup> It revises some of the original decisions such as the border points, and produces a hierarchical instead of a flat result.

In 1972, Robert F. Ling published a closely related algorithm in "The Theory and Construction of k-Clusters"<sup id="cite_ref-:1_8-0"><a href="https://en.wikipedia.org/wiki/DBSCAN#cite_note-:1-8"><span>[</span>8<span>]</span></a></sup> in _[The Computer Journal](https://en.wikipedia.org/wiki/The_Computer_Journal "The Computer Journal")_ with an estimated runtime complexity of O(n³).<sup id="cite_ref-:1_8-1"><a href="https://en.wikipedia.org/wiki/DBSCAN#cite_note-:1-8"><span>[</span>8<span>]</span></a></sup> DBSCAN has a worst-case of O(n²), and the database-oriented range-query formulation of DBSCAN allows for index acceleration. The algorithms slightly differ in their handling of border points.

Consider a set of points in some space to be clustered. Let ε be a parameter specifying the radius of a neighborhood with respect to some point. For the purpose of DBSCAN clustering, the points are classified as _core points_, (_directly_\-) _reachable points_ and _outliers_, as follows:

-   A point p is a _core point_ if at least minPts points are within distance ε of it (including p).
-   A point q is _directly reachable_ from p if point q is within distance ε from core point p. Points are only said to be directly reachable from core points.
-   A point q is _reachable_ from p if there is a path _p_<sub>1</sub>, ..., _p<sub>n</sub>_ with _p_<sub>1</sub> = _p_ and _p<sub>n</sub>_ = _q_, where each _p_<sub><i>i</i>+1</sub> is directly reachable from p<sub>i</sub>. Note that this implies that the initial point and all points on the path must be core points, with the possible exception of q.
-   All points not reachable from any other point are _outliers_ or _noise points_.

Now if p is a core point, then it forms a _cluster_ together with all points (core or non-core) that are reachable from it. Each cluster contains at least one core point; non-core points can be part of a cluster, but they form its "edge", since they cannot be used to reach more points.

[![](https://upload.wikimedia.org/wikipedia/commons/thumb/a/af/DBSCAN-Illustration.svg/500px-DBSCAN-Illustration.svg.png)](https://en.wikipedia.org/wiki/File:DBSCAN-Illustration.svg)

In this diagram, minPts = 4. Point A and the other red points are core points, because the area surrounding these points in an ε radius contain at least 4 points (including the point itself). Because they are all reachable from one another, they form a single cluster. Points B and C are not core points, but are reachable from A (via other core points) and thus belong to the cluster as well. Point N is a noise point that is neither a core point nor directly reachable.

Reachability is not a symmetric relation: by definition, only core points can reach non-core points. The opposite is not true, so a non-core point may be reachable, but nothing can be reached from it. Therefore, a further notion of _connectedness_ is needed to formally define the extent of the clusters found by DBSCAN. Two points p and q are density-connected if there is a point o such that both p and q are reachable from o. Density-connectedness _is_ symmetric.

A cluster then satisfies two properties:

1.  All points within the cluster are mutually density-connected.
2.  If a point is density-reachable from some point of the cluster, it is part of the cluster as well.

### Original query-based algorithm

\[[edit](https://en.wikipedia.org/w/index.php?title=DBSCAN&action=edit&section=4 "Edit section: Original query-based algorithm")\]

DBSCAN requires two parameters: ε (eps) and the minimum number of points required to form a dense region<sup id="cite_ref-minpts_9-0"><a href="https://en.wikipedia.org/wiki/DBSCAN#cite_note-minpts-9"><span>[</span>a<span>]</span></a></sup> (minPts). It starts with an arbitrary starting point that has not been visited. This point's ε-neighborhood is retrieved, and if it contains sufficiently many points, a cluster is started. Otherwise, the point is labeled as noise. Note that this point might later be found in a sufficiently sized ε-environment of a different point and hence be made part of a cluster.

If a point is found to be a dense part of a cluster, its ε-neighborhood is also part of that cluster. Hence, all points that are found within the ε-neighborhood are added, as is their own ε-neighborhood when they are also dense. This process continues until the density-connected cluster is completely found. Then, a new unvisited point is retrieved and processed, leading to the discovery of a further cluster or noise.

DBSCAN can be used with any distance function<sup id="cite_ref-dbscan_1-1"><a href="https://en.wikipedia.org/wiki/DBSCAN#cite_note-dbscan-1"><span>[</span>1<span>]</span></a></sup><sup id="cite_ref-tods_4-3"><a href="https://en.wikipedia.org/wiki/DBSCAN#cite_note-tods-4"><span>[</span>4<span>]</span></a></sup> (as well as similarity functions or other predicates).<sup id="cite_ref-:0_10-0"><a href="https://en.wikipedia.org/wiki/DBSCAN#cite_note-:0-10"><span>[</span>9<span>]</span></a></sup> The distance function (dist) can therefore be seen as an additional parameter.

The algorithm can be expressed in [pseudocode](https://en.wikipedia.org/wiki/Pseudocode "Pseudocode") as follows:<sup id="cite_ref-tods_4-4"><a href="https://en.wikipedia.org/wiki/DBSCAN#cite_note-tods-4"><span>[</span>4<span>]</span></a></sup>

```
DBSCAN(DB, distFunc, eps, minPts) {
    C := 0                                                  /* Cluster counter */
    for each point P in database DB {
        if label(P) ≠ undefined then continue               /* Previously processed in inner loop */
        Neighbors N := RangeQuery(DB, distFunc, P, eps)     /* Find neighbors */
        if |N| < minPts then {                              /* Density check */
            label(P) := Noise                               /* Label as Noise */
            continue
        }
        C := C + 1                                          /* next cluster label */
        label(P) := C                                       /* Label initial point */
        SeedSet S := N \ {P}                                /* Neighbors to expand */
        for each point Q in S {                             /* Process every seed point Q */
            if label(Q) = Noise then label(Q) := C          /* Change Noise to border point */
            if label(Q) ≠ undefined then continue           /* Previously processed (e.g., border point) */
            label(Q) := C                                   /* Label neighbor */
            Neighbors N := RangeQuery(DB, distFunc, Q, eps) /* Find neighbors */
            if |N| ≥ minPts then {                          /* Density check (if Q is a core point) */
                S := S ∪ N                                  /* Add new neighbors to seed set */
            }
        }
    }
}

```

where RangeQuery can be implemented using a database index for better performance, or using a slow linear scan:

```
RangeQuery(DB, distFunc, Q, eps) {
    Neighbors N := empty list
    for each point P in database DB {                      /* Scan all points in the database */
        if distFunc(Q, P) ≤ eps then {                     /* Compute distance and check epsilon */
            N := N ∪ {P}                                   /* Add to result */
        }
    }
    return N
}

```

The DBSCAN algorithm can be abstracted into the following steps:<sup id="cite_ref-tods_4-5"><a href="https://en.wikipedia.org/wiki/DBSCAN#cite_note-tods-4"><span>[</span>4<span>]</span></a></sup>

1.  Find the points in the ε (eps) neighborhood of every point, and identify the core points with more than minPts neighbors.
2.  Find the [connected components](https://en.wikipedia.org/wiki/Connected_component_(graph_theory) "Connected component (graph theory)") of _core_ points on the neighbor graph, ignoring all non-core points.
3.  Assign each non-core point to a nearby cluster if the cluster is an ε (eps) neighbor, otherwise assign it to noise.

A naive implementation of this requires storing the neighborhoods in step 1, thus requiring substantial memory. The original DBSCAN algorithm does not require this by performing these steps for one point at a time.

### Optimization Criterion

\[[edit](https://en.wikipedia.org/w/index.php?title=DBSCAN&action=edit&section=6 "Edit section: Optimization Criterion")\]

DBSCAN optimizes the following loss function:<sup id="cite_ref-11"><a href="https://en.wikipedia.org/wiki/DBSCAN#cite_note-11"><span>[</span>10<span>]</span></a></sup> For any possible clustering ![{\displaystyle C=\{C_{1},\ldots ,C_{l}\}}](https://wikimedia.org/api/rest_v1/media/math/render/svg/84289705276034a6a07e8ec1dc29f0cfc3989a07) out of the set of all clusterings ![{\displaystyle {\mathcal {C}}}](https://wikimedia.org/api/rest_v1/media/math/render/svg/e7b3edab7022ca9e2976651bc59c489513ee9019), it minimizes the number of clusters under the condition that every pair of points in a cluster is density-reachable, which corresponds to the original two properties "maximality" and "connectivity" of a cluster:<sup id="cite_ref-dbscan_1-2"><a href="https://en.wikipedia.org/wiki/DBSCAN#cite_note-dbscan-1"><span>[</span>1<span>]</span></a></sup>

![{\displaystyle \min _{C\subset {\mathcal {C}},~d_{db}(p,q)\leq \varepsilon ~\forall p,q\in C_{i}~\forall C_{i}\in C}|C|}](https://wikimedia.org/api/rest_v1/media/math/render/svg/f9114b8f5687465e1e8626fd7799164ff2df5d4f)

where ![{\displaystyle d_{db}(p,q)}](https://wikimedia.org/api/rest_v1/media/math/render/svg/92570ee27c7bbda8b0d4ca09067f912be81d23d9) gives the smallest ![{\displaystyle \varepsilon }](https://wikimedia.org/api/rest_v1/media/math/render/svg/a30c89172e5b88edbd45d3e2772c7f5e562e5173) such that two points p and q are density-connected.

DBSCAN visits each point of the database, possibly multiple times (e.g., as candidates to different clusters). For practical considerations, however, the time complexity is mostly governed by the number of regionQuery invocations. DBSCAN executes exactly one such query for each point, and if an [indexing structure](https://en.wikipedia.org/wiki/Spatial_index "Spatial index") is used that executes a [neighborhood query](https://en.wikipedia.org/wiki/Fixed-radius_near_neighbors "Fixed-radius near neighbors") in O(log _n_), an overall average runtime complexity of O(_n_ log _n_) is obtained (if parameter ε is chosen in a meaningful way, i.e. such that on average only O(log _n_) points are returned). Without the use of an accelerating index structure, or on degenerated data (e.g. all points within a distance less than ε), the worst case run time complexity remains O(_n_²). The ![{\displaystyle \textstyle {\binom {n}{2}}}](https://wikimedia.org/api/rest_v1/media/math/render/svg/eb93e0b40fb374f394b62739ef1c2529fb316431) \- _n_ = (_n_²-_n_)/2\-sized upper triangle of the distance matrix can be materialized to avoid distance recomputations, but this needs O(_n_²) memory, whereas a non-matrix based implementation of DBSCAN only needs O(_n_) memory.

[![](https://upload.wikimedia.org/wikipedia/commons/thumb/0/05/DBSCAN-density-data.svg/250px-DBSCAN-density-data.svg.png)](https://en.wikipedia.org/wiki/File:DBSCAN-density-data.svg)

DBSCAN can find non-linearly separable clusters. This dataset cannot be adequately clustered with k-means or Gaussian Mixture EM clustering.

1.  DBSCAN does not require one to specify the number of clusters in the data a priori, as opposed to [k-means](https://en.wikipedia.org/wiki/K-means_algorithm "K-means algorithm").
2.  DBSCAN can find arbitrarily-shaped clusters. It can even find a cluster completely surrounded by (but not connected to) a different cluster. Due to the MinPts parameter, the so-called single-link effect (different clusters being connected by a thin line of points) is reduced.
3.  DBSCAN has a notion of noise, and is robust to [outliers](https://en.wikipedia.org/wiki/Anomaly_detection "Anomaly detection").
4.  DBSCAN requires just two parameters and is mostly insensitive to the ordering of the points in the database. (However, points sitting on the edge of two different clusters might swap cluster membership if the ordering of the points is changed, and the cluster assignment is unique only up to isomorphism.)
5.  DBSCAN is designed for use with databases that can accelerate region queries, e.g. using an [R\* tree](https://en.wikipedia.org/wiki/R*_tree "R* tree").
6.  The parameters minPts and ε can be set by a domain expert, if the data is well understood.

1.  DBSCAN is not entirely deterministic: border points that are reachable from more than one cluster can be part of either cluster, depending on the order the data are processed. For most data sets and domains, this situation does not arise often and has little impact on the clustering result:<sup id="cite_ref-tods_4-6"><a href="https://en.wikipedia.org/wiki/DBSCAN#cite_note-tods-4"><span>[</span>4<span>]</span></a></sup> both on core points and noise points, DBSCAN is deterministic. DBSCAN\*<sup id="cite_ref-hdbscan1_6-1"><a href="https://en.wikipedia.org/wiki/DBSCAN#cite_note-hdbscan1-6"><span>[</span>6<span>]</span></a></sup><sup id="cite_ref-hdbscan2_7-1"><a href="https://en.wikipedia.org/wiki/DBSCAN#cite_note-hdbscan2-7"><span>[</span>7<span>]</span></a></sup> is a variation that treats border points as noise, and this way achieves a fully deterministic result as well as a more consistent statistical interpretation of density-connected components.
2.  The quality of DBSCAN depends on the [distance measure](https://en.wikipedia.org/wiki/Metric_(mathematics) "Metric (mathematics)") used in the function regionQuery(P,ε). The most common distance metric used is [Euclidean distance](https://en.wikipedia.org/wiki/Euclidean_distance "Euclidean distance"). Especially for [high-dimensional data](https://en.wikipedia.org/wiki/Clustering_high-dimensional_data "Clustering high-dimensional data"), this metric can be rendered almost useless due to the so-called "[Curse of dimensionality](https://en.wikipedia.org/wiki/Curse_of_dimensionality#Distance_functions "Curse of dimensionality")", making it difficult to find an appropriate value for ε. This effect, however, is also present in any other algorithm based on Euclidean distance.
3.  DBSCAN cannot cluster data sets well with large differences in densities, since the minPts-ε combination cannot then be chosen appropriately for all clusters.<sup id="cite_ref-WIREs_12-0"><a href="https://en.wikipedia.org/wiki/DBSCAN#cite_note-WIREs-12"><span>[</span>11<span>]</span></a></sup>
4.  If the data and scale are not well understood, choosing a meaningful distance threshold ε can be difficult.

See the section below on extensions for algorithmic modifications to handle these issues.

## Parameter estimation

\[[edit](https://en.wikipedia.org/w/index.php?title=DBSCAN&action=edit&section=10 "Edit section: Parameter estimation")\]

Every data mining task has the problem of parameters. Every parameter influences the algorithm in specific ways. For DBSCAN, the parameters ε and _minPts_ are needed. The parameters must be specified by the user. Ideally, the value of ε is given by the problem to solve (e.g. a physical distance), and _minPts_ is then the desired minimum cluster size.<sup id="cite_ref-minpts_9-1"><a href="https://en.wikipedia.org/wiki/DBSCAN#cite_note-minpts-9"><span>[</span>a<span>]</span></a></sup>

-   _MinPts_: As a rule of thumb, a minimum _minPts_ can be derived from the number of dimensions _D_ in the data set, as _minPts_ ≥ _D_ + 1. The low value of _minPts_ = 1 does not make sense, as then every point is a core point by definition. With _minPts_ ≤ 2, the result will be the same as of [hierarchical clustering](https://en.wikipedia.org/wiki/Hierarchical_clustering "Hierarchical clustering") with the single link metric, with the dendrogram cut at height ε. Therefore, _minPts_ must be chosen at least 3. However, larger values are usually better for data sets with noise and will yield more significant clusters. As a rule of thumb, _minPts_ = 2·_dim_ can be used,<sup id="cite_ref-:0_10-1"><a href="https://en.wikipedia.org/wiki/DBSCAN#cite_note-:0-10"><span>[</span>9<span>]</span></a></sup> but it may be necessary to choose larger values for very large data, for noisy data or for data that contains many duplicates.<sup id="cite_ref-tods_4-7"><a href="https://en.wikipedia.org/wiki/DBSCAN#cite_note-tods-4"><span>[</span>4<span>]</span></a></sup>
-   ε: The value for ε can then be chosen by using a [k-distance graph](https://en.wikipedia.org/wiki/Nearest_neighbor_graph "Nearest neighbor graph"), plotting the distance to the _k_ = _minPts_\-1 nearest neighbor ordered from the largest to the smallest value.<sup id="cite_ref-tods_4-8"><a href="https://en.wikipedia.org/wiki/DBSCAN#cite_note-tods-4"><span>[</span>4<span>]</span></a></sup> Good values of ε are where this plot shows an "elbow":<sup id="cite_ref-dbscan_1-3"><a href="https://en.wikipedia.org/wiki/DBSCAN#cite_note-dbscan-1"><span>[</span>1<span>]</span></a></sup><sup id="cite_ref-:0_10-2"><a href="https://en.wikipedia.org/wiki/DBSCAN#cite_note-:0-10"><span>[</span>9<span>]</span></a></sup><sup id="cite_ref-tods_4-9"><a href="https://en.wikipedia.org/wiki/DBSCAN#cite_note-tods-4"><span>[</span>4<span>]</span></a></sup> if ε is chosen much too small, a large part of the data will not be clustered; whereas for a too high value of ε, clusters will merge and the majority of objects will be in the same cluster. In general, small values of ε are preferable,<sup id="cite_ref-tods_4-10"><a href="https://en.wikipedia.org/wiki/DBSCAN#cite_note-tods-4"><span>[</span>4<span>]</span></a></sup> and as a rule of thumb only a small fraction of points should be within this distance of each other. Alternatively, an [OPTICS](https://en.wikipedia.org/wiki/OPTICS_algorithm "OPTICS algorithm") plot can be used to choose ε,<sup id="cite_ref-tods_4-11"><a href="https://en.wikipedia.org/wiki/DBSCAN#cite_note-tods-4"><span>[</span>4<span>]</span></a></sup> but then the OPTICS algorithm itself can be used to cluster the data.
-   Distance function: The choice of distance function is tightly coupled to the choice of ε, and has a major impact on the results. In general, it will be necessary to first identify a reasonable measure of similarity for the data set, before the parameter ε can be chosen. There is no estimation for this parameter, but the distance functions needs to be chosen appropriately for the data set. For example, on geographic data, the [great-circle distance](https://en.wikipedia.org/wiki/Great-circle_distance "Great-circle distance") is often a good choice.

[OPTICS](https://en.wikipedia.org/wiki/OPTICS_algorithm "OPTICS algorithm") can be seen as a generalization of DBSCAN that replaces the ε parameter with a maximum value that mostly affects performance. _MinPts_ then essentially becomes the minimum cluster size to find. While the algorithm is much easier to parameterize than DBSCAN, the results are a bit more difficult to use, as it will usually produce a hierarchical clustering instead of the simple data partitioning that DBSCAN produces.

Recently, one of the original authors of DBSCAN has revisited DBSCAN and OPTICS, and published a refined version of hierarchical DBSCAN (HDBSCAN\*),<sup id="cite_ref-hdbscan1_6-2"><a href="https://en.wikipedia.org/wiki/DBSCAN#cite_note-hdbscan1-6"><span>[</span>6<span>]</span></a></sup><sup id="cite_ref-hdbscan2_7-2"><a href="https://en.wikipedia.org/wiki/DBSCAN#cite_note-hdbscan2-7"><span>[</span>7<span>]</span></a></sup> which no longer has the notion of border points. Instead, only the core points form the cluster.

## Relationship to spectral clustering

\[[edit](https://en.wikipedia.org/w/index.php?title=DBSCAN&action=edit&section=11 "Edit section: Relationship to spectral clustering")\]

A spectral implementation of DBSCAN is related to [spectral clustering](https://en.wikipedia.org/wiki/Spectral_clustering "Spectral clustering") in the trivial case of determining [connected graph components](https://en.wikipedia.org/wiki/Connected_component_(graph_theory) "Connected component (graph theory)") — the optimal clusters with no edges cut.<sup id="cite_ref-13"><a href="https://en.wikipedia.org/wiki/DBSCAN#cite_note-13"><span>[</span>12<span>]</span></a></sup> However, it can be computationally intensive, up to ![{\displaystyle O(n^{3})}](https://wikimedia.org/api/rest_v1/media/math/render/svg/6b04f5c5cfea38f43406d9442387ad28555e2609). Additionally, one has to choose the number of eigenvectors to compute. For performance reasons, the original DBSCAN algorithm remains preferable to its spectral implementation.

Generalized DBSCAN (GDBSCAN)<sup id="cite_ref-:0_10-3"><a href="https://en.wikipedia.org/wiki/DBSCAN#cite_note-:0-10"><span>[</span>9<span>]</span></a></sup><sup id="cite_ref-14"><a href="https://en.wikipedia.org/wiki/DBSCAN#cite_note-14"><span>[</span>13<span>]</span></a></sup> is a generalization by the same authors to arbitrary "neighborhood" and "dense" predicates. The ε and _minPts_ parameters are removed from the original algorithm and moved to the predicates. For example, on polygon data, the "neighborhood" could be any intersecting polygon, whereas the density predicate uses the polygon areas instead of just the object count.

Various extensions to the DBSCAN algorithm have been proposed, including methods for parallelization, parameter estimation, and support for uncertain data. The basic idea has been extended to hierarchical clustering by the [OPTICS algorithm](https://en.wikipedia.org/wiki/OPTICS_algorithm "OPTICS algorithm"). DBSCAN is also used as part of subspace clustering algorithms like PreDeCon and [SUBCLU](https://en.wikipedia.org/wiki/SUBCLU "SUBCLU"). HDBSCAN\*<sup id="cite_ref-hdbscan1_6-3"><a href="https://en.wikipedia.org/wiki/DBSCAN#cite_note-hdbscan1-6"><span>[</span>6<span>]</span></a></sup><sup id="cite_ref-hdbscan2_7-3"><a href="https://en.wikipedia.org/wiki/DBSCAN#cite_note-hdbscan2-7"><span>[</span>7<span>]</span></a></sup> is a hierarchical version of DBSCAN which is also faster than OPTICS, from which a flat partition consisting of the most prominent clusters can be extracted from the hierarchy.<sup id="cite_ref-15"><a href="https://en.wikipedia.org/wiki/DBSCAN#cite_note-15"><span>[</span>14<span>]</span></a></sup>

Different implementations of the same algorithm were found to exhibit enormous performance differences, with the fastest on a test data set finishing in 1.4 seconds, the slowest taking 13803 seconds.<sup id="cite_ref-16"><a href="https://en.wikipedia.org/wiki/DBSCAN#cite_note-16"><span>[</span>15<span>]</span></a></sup> The differences can be attributed to implementation quality, language and compiler differences, and the use of indexes for acceleration.

-   [Apache Commons](https://en.wikipedia.org/wiki/Apache_Commons "Apache Commons") [Math](https://commons.apache.org/proper/commons-math/) contains a Java implementation of the algorithm running in quadratic time.
-   [ELKI](https://en.wikipedia.org/wiki/ELKI "ELKI") offers an implementation of DBSCAN as well as GDBSCAN and other variants. This implementation can use various index structures for sub-quadratic runtime and supports arbitrary distance functions and arbitrary data types, but it may be outperformed by low-level optimized (and specialized) implementations on small data sets.
-   [mlpack](https://en.wikipedia.org/wiki/Mlpack "Mlpack") includes an implementation of DBSCAN accelerated with dual-tree range search techniques.
-   [PostGIS](https://en.wikipedia.org/wiki/PostGIS "PostGIS") includes ST\_ClusterDBSCAN – a 2D implementation of DBSCAN that uses R-tree index. Any geometry type is supported, e.g. Point, LineString, Polygon, etc.
-   [R](https://en.wikipedia.org/wiki/R_(programming_language) "R (programming language)") contains implementations of DBSCAN in the packages [dbscan](https://cran.r-project.org/package=dbscan) and [fpc](https://cran.r-project.org/package=fpc). Both packages support arbitrary distance functions via distance matrices. The package fpc does not have index support (and thus has quadratic runtime and memory complexity) and is rather slow due to the R interpreter. The package dbscan provides a fast C++ implementation using [k-d trees](https://en.wikipedia.org/wiki/K-d_tree "K-d tree") (for Euclidean distance only) and also includes implementations of DBSCAN\*, HDBSCAN\*, OPTICS, OPTICSXi, and other related methods.
-   [scikit-learn](https://en.wikipedia.org/wiki/Scikit-learn "Scikit-learn") includes a Python implementation of DBSCAN for arbitrary [Minkowski metrics](https://en.wikipedia.org/wiki/Minkowski_distance "Minkowski distance"), which can be accelerated using [k-d trees](https://en.wikipedia.org/wiki/K-d_tree "K-d tree") and [ball trees](https://en.wikipedia.org/wiki/Ball_tree "Ball tree") but which uses worst-case quadratic memory. A [contribution to scikit-learn](https://github.com/scikit-learn-contrib/hdbscan) provides an implementation of the HDBSCAN\* algorithm.
-   [pyclustering](https://github.com/annoviko/pyclustering) library includes a Python and C++ implementation of DBSCAN for Euclidean distance only as well as OPTICS algorithm.
-   [SPMF](http://www.philippe-fournier-viger.com/spmf/) includes an implementation of the DBSCAN algorithm with k-d tree support for Euclidean distance only.
-   [Weka](https://en.wikipedia.org/wiki/Weka_(machine_learning) "Weka (machine learning)") contains (as an optional package in latest versions) a basic implementation of DBSCAN that runs in quadratic time and linear memory.
-   [linfa](https://docs.rs/linfa-clustering/latest/linfa_clustering/struct.Dbscan.html) includes an implementation of the DBSCAN for the [rust programming language](https://en.wikipedia.org/wiki/Rust_(programming_language) "Rust (programming language)").
-   [Julia](https://en.wikipedia.org/wiki/Julia_(programming_language) "Julia (programming language)") includes an implementation of DBSCAN in the Julia Statistics's Clustering.jl package.

-   [Cluster analysis](https://en.wikipedia.org/wiki/Cluster_analysis "Cluster analysis") – Grouping a set of objects by similarity
-   [k-means clustering](https://en.wikipedia.org/wiki/K-means_clustering "K-means clustering") – Vector quantization algorithm minimizing the sum of squared deviations

1.  ^ [Jump up to: <sup><i><b>a</b></i></sup>](https://en.wikipedia.org/wiki/DBSCAN#cite_ref-minpts_9-0) [<sup><i><b>b</b></i></sup>](https://en.wikipedia.org/wiki/DBSCAN#cite_ref-minpts_9-1) While minPts intuitively is the minimum cluster size, in some cases DBSCAN _can_ produce smaller clusters.<sup id="cite_ref-tods_4-1"><a href="https://en.wikipedia.org/wiki/DBSCAN#cite_note-tods-4"><span>[</span>4<span>]</span></a></sup> A DBSCAN cluster consists of at least _one core point_.<sup id="cite_ref-tods_4-2"><a href="https://en.wikipedia.org/wiki/DBSCAN#cite_note-tods-4"><span>[</span>4<span>]</span></a></sup> As other points may be border points to more than one cluster, there is no guarantee that at least minPts points are included in every cluster.

1.  ^ [Jump up to: <sup><i><b>a</b></i></sup>](https://en.wikipedia.org/wiki/DBSCAN#cite_ref-dbscan_1-0) [<sup><i><b>b</b></i></sup>](https://en.wikipedia.org/wiki/DBSCAN#cite_ref-dbscan_1-1) [<sup><i><b>c</b></i></sup>](https://en.wikipedia.org/wiki/DBSCAN#cite_ref-dbscan_1-2) [<sup><i><b>d</b></i></sup>](https://en.wikipedia.org/wiki/DBSCAN#cite_ref-dbscan_1-3) [Ester, Martin](https://en.wikipedia.org/wiki/Martin_Ester "Martin Ester"); [Kriegel, Hans-Peter](https://en.wikipedia.org/wiki/Hans-Peter_Kriegel "Hans-Peter Kriegel"); [Sander, Jörg](https://en.wikipedia.org/w/index.php?title=J%C3%B6rg_Sander&action=edit&redlink=1 "Jörg Sander (page does not exist)"); [Xu, Xiaowei](https://en.wikipedia.org/w/index.php?title=Xiaowei_Xu&action=edit&redlink=1 "Xiaowei Xu (page does not exist)") (1996). Simoudis, Evangelos; Han, Jiawei; Fayyad, Usama M. (eds.). [_A density-based algorithm for discovering clusters in large spatial databases with noise_](https://cdn.aaai.org/KDD/1996/KDD96-037.pdf) (PDF). Proceedings of the Second International Conference on Knowledge Discovery and Data Mining (KDD-96). [AAAI Press](https://en.wikipedia.org/wiki/AAAI_Press "AAAI Press"). pp. 226–231\. [CiteSeerX](https://en.wikipedia.org/wiki/CiteSeerX_(identifier) "CiteSeerX (identifier)") [10.1.1.121.9220](https://citeseerx.ist.psu.edu/viewdoc/summary?doi=10.1.1.121.9220). [ISBN](https://en.wikipedia.org/wiki/ISBN_(identifier) "ISBN (identifier)") [1-57735-004-9](https://en.wikipedia.org/wiki/Special:BookSources/1-57735-004-9 "Special:BookSources/1-57735-004-9").
2.  **[^](https://en.wikipedia.org/wiki/DBSCAN#cite_ref-2 "Jump up")** ["Microsoft Academic Search: Papers"](https://web.archive.org/web/20100421170848/http://academic.research.microsoft.com/CSDirectory/paper_category_7.htm). Archived from [the original](http://academic.research.microsoft.com/CSDirectory/paper_category_7.htm) on April 21, 2010. Retrieved 2010-04-18. Most cited data mining articles according to Microsoft academic search; DBSCAN is on rank 24.
3.  **[^](https://en.wikipedia.org/wiki/DBSCAN#cite_ref-3 "Jump up")** ["2014 SIGKDD Test of Time Award"](http://www.kdd.org/News/view/2014-sigkdd-test-of-time-award). [ACM](https://en.wikipedia.org/wiki/Association_for_Computing_Machinery "Association for Computing Machinery") SIGKDD. 2014-08-18. Retrieved 2016-07-27.
4.  ^ [Jump up to: <sup><i><b>a</b></i></sup>](https://en.wikipedia.org/wiki/DBSCAN#cite_ref-tods_4-0) [<sup><i><b>b</b></i></sup>](https://en.wikipedia.org/wiki/DBSCAN#cite_ref-tods_4-1) [<sup><i><b>c</b></i></sup>](https://en.wikipedia.org/wiki/DBSCAN#cite_ref-tods_4-2) [<sup><i><b>d</b></i></sup>](https://en.wikipedia.org/wiki/DBSCAN#cite_ref-tods_4-3) [<sup><i><b>e</b></i></sup>](https://en.wikipedia.org/wiki/DBSCAN#cite_ref-tods_4-4) [<sup><i><b>f</b></i></sup>](https://en.wikipedia.org/wiki/DBSCAN#cite_ref-tods_4-5) [<sup><i><b>g</b></i></sup>](https://en.wikipedia.org/wiki/DBSCAN#cite_ref-tods_4-6) [<sup><i><b>h</b></i></sup>](https://en.wikipedia.org/wiki/DBSCAN#cite_ref-tods_4-7) [<sup><i><b>i</b></i></sup>](https://en.wikipedia.org/wiki/DBSCAN#cite_ref-tods_4-8) [<sup><i><b>j</b></i></sup>](https://en.wikipedia.org/wiki/DBSCAN#cite_ref-tods_4-9) [<sup><i><b>k</b></i></sup>](https://en.wikipedia.org/wiki/DBSCAN#cite_ref-tods_4-10) [<sup><i><b>l</b></i></sup>](https://en.wikipedia.org/wiki/DBSCAN#cite_ref-tods_4-11) [Schubert, Erich](https://en.wikipedia.org/w/index.php?title=Erich_Schubert&action=edit&redlink=1 "Erich Schubert (page does not exist)"); [Sander, Jörg](https://en.wikipedia.org/w/index.php?title=J%C3%B6rg_Sander&action=edit&redlink=1 "Jörg Sander (page does not exist)"); [Ester, Martin](https://en.wikipedia.org/w/index.php?title=Martin_Erster&action=edit&redlink=1 "Martin Erster (page does not exist)"); [Kriegel, Hans Peter](https://en.wikipedia.org/wiki/Hans-Peter_Kriegel "Hans-Peter Kriegel"); [Xu, Xiaowei](https://en.wikipedia.org/w/index.php?title=Xiaowei_Xu&action=edit&redlink=1 "Xiaowei Xu (page does not exist)") (July 2017). ["DBSCAN Revisited, Revisited: Why and How You Should (Still) Use DBSCAN"](https://www.vitavonni.de/research/acm.html#item3068335). _ACM Trans. Database Syst_. **42** (3): 19:1–19:21. [doi](https://en.wikipedia.org/wiki/Doi_(identifier) "Doi (identifier)"):[10.1145/3068335](https://doi.org/10.1145%2F3068335). [ISSN](https://en.wikipedia.org/wiki/ISSN_(identifier) "ISSN (identifier)") [0362-5915](https://search.worldcat.org/issn/0362-5915). [S2CID](https://en.wikipedia.org/wiki/S2CID_(identifier) "S2CID (identifier)") [5156876](https://api.semanticscholar.org/CorpusID:5156876).
5.  **[^](https://en.wikipedia.org/wiki/DBSCAN#cite_ref-5 "Jump up")** ["TODS Home"](https://dl.acm.org/journal/tods#pane-cc36fa1b-c28c-425f-9325-f853cff8882901). _tods.acm.org_. [Association for Computing Machinery](https://en.wikipedia.org/wiki/Association_for_Computing_Machinery "Association for Computing Machinery"). Retrieved 2020-07-16.
6.  ^ [Jump up to: <sup><i><b>a</b></i></sup>](https://en.wikipedia.org/wiki/DBSCAN#cite_ref-hdbscan1_6-0) [<sup><i><b>b</b></i></sup>](https://en.wikipedia.org/wiki/DBSCAN#cite_ref-hdbscan1_6-1) [<sup><i><b>c</b></i></sup>](https://en.wikipedia.org/wiki/DBSCAN#cite_ref-hdbscan1_6-2) [<sup><i><b>d</b></i></sup>](https://en.wikipedia.org/wiki/DBSCAN#cite_ref-hdbscan1_6-3) Campello, Ricardo J. G. B.; Moulavi, Davoud; [Sander, Joerg](https://en.wikipedia.org/w/index.php?title=J%C3%B6rg_Sander&action=edit&redlink=1 "Jörg Sander (page does not exist)") (2013). Pei, Jian; Tseng, Vincent S.; Cao, Longbing; Motoda, Hiroshi (eds.). [_Density-Based Clustering Based on Hierarchical Density Estimates_](http://link.springer.com/10.1007/978-3-642-37456-2_14). Advances in Knowledge Discovery and Data Mining. Vol. 7819. Berlin, Heidelberg: Springer Berlin Heidelberg. pp. 160–172\. [doi](https://en.wikipedia.org/wiki/Doi_(identifier) "Doi (identifier)"):[10.1007/978-3-642-37456-2\_14](https://doi.org/10.1007%2F978-3-642-37456-2_14). [ISBN](https://en.wikipedia.org/wiki/ISBN_(identifier) "ISBN (identifier)") [978-3-642-37455-5](https://en.wikipedia.org/wiki/Special:BookSources/978-3-642-37455-5 "Special:BookSources/978-3-642-37455-5"). Retrieved 2023-08-18.
7.  ^ [Jump up to: <sup><i><b>a</b></i></sup>](https://en.wikipedia.org/wiki/DBSCAN#cite_ref-hdbscan2_7-0) [<sup><i><b>b</b></i></sup>](https://en.wikipedia.org/wiki/DBSCAN#cite_ref-hdbscan2_7-1) [<sup><i><b>c</b></i></sup>](https://en.wikipedia.org/wiki/DBSCAN#cite_ref-hdbscan2_7-2) [<sup><i><b>d</b></i></sup>](https://en.wikipedia.org/wiki/DBSCAN#cite_ref-hdbscan2_7-3) Campello, Ricardo J. G. B.; Moulavi, Davoud; [Zimek, Arthur](https://en.wikipedia.org/wiki/Arthur_Zimek "Arthur Zimek"); [Sander, Jörg](https://en.wikipedia.org/w/index.php?title=J%C3%B6rg_Sander&action=edit&redlink=1 "Jörg Sander (page does not exist)") (2015). "Hierarchical Density Estimates for Data Clustering, Visualization, and Outlier Detection". _ACM Transactions on Knowledge Discovery from Data_. **10** (1): 1–51\. [doi](https://en.wikipedia.org/wiki/Doi_(identifier) "Doi (identifier)"):[10.1145/2733381](https://doi.org/10.1145%2F2733381). [ISSN](https://en.wikipedia.org/wiki/ISSN_(identifier) "ISSN (identifier)") [1556-4681](https://search.worldcat.org/issn/1556-4681). [S2CID](https://en.wikipedia.org/wiki/S2CID_(identifier) "S2CID (identifier)") [2887636](https://api.semanticscholar.org/CorpusID:2887636).
8.  ^ [Jump up to: <sup><i><b>a</b></i></sup>](https://en.wikipedia.org/wiki/DBSCAN#cite_ref-:1_8-0) [<sup><i><b>b</b></i></sup>](https://en.wikipedia.org/wiki/DBSCAN#cite_ref-:1_8-1) Ling, R. F. (1972-01-01). ["On the theory and construction of k-clusters"](https://academic.oup.com/comjnl/article/15/4/326/351493). _The Computer Journal_. **15** (4): 326–332\. [doi](https://en.wikipedia.org/wiki/Doi_(identifier) "Doi (identifier)"):[10.1093/comjnl/15.4.326](https://doi.org/10.1093%2Fcomjnl%2F15.4.326). [ISSN](https://en.wikipedia.org/wiki/ISSN_(identifier) "ISSN (identifier)") [0010-4620](https://search.worldcat.org/issn/0010-4620).
9.  ^ [Jump up to: <sup><i><b>a</b></i></sup>](https://en.wikipedia.org/wiki/DBSCAN#cite_ref-:0_10-0) [<sup><i><b>b</b></i></sup>](https://en.wikipedia.org/wiki/DBSCAN#cite_ref-:0_10-1) [<sup><i><b>c</b></i></sup>](https://en.wikipedia.org/wiki/DBSCAN#cite_ref-:0_10-2) [<sup><i><b>d</b></i></sup>](https://en.wikipedia.org/wiki/DBSCAN#cite_ref-:0_10-3) [Sander, Jörg](https://en.wikipedia.org/w/index.php?title=J%C3%B6rg_Sander&action=edit&redlink=1 "Jörg Sander (page does not exist)"); [Ester, Martin](https://en.wikipedia.org/wiki/Martin_Ester "Martin Ester"); [Kriegel, Hans-Peter](https://en.wikipedia.org/wiki/Hans-Peter_Kriegel "Hans-Peter Kriegel"); [Xu, Xiaowei](https://en.wikipedia.org/w/index.php?title=Xiaowei_Xu&action=edit&redlink=1 "Xiaowei Xu (page does not exist)") (1998). "Density-Based Clustering in Spatial Databases: The Algorithm GDBSCAN and Its Applications". _Data Mining and Knowledge Discovery_. **2** (2). Berlin: [Springer-Verlag](https://en.wikipedia.org/wiki/Springer-Verlag "Springer-Verlag"): 169–194\. [Bibcode](https://en.wikipedia.org/wiki/Bibcode_(identifier) "Bibcode (identifier)"):[1998DMKD....2..169S](https://ui.adsabs.harvard.edu/abs/1998DMKD....2..169S). [doi](https://en.wikipedia.org/wiki/Doi_(identifier) "Doi (identifier)"):[10.1023/A:1009745219419](https://doi.org/10.1023%2FA%3A1009745219419). [S2CID](https://en.wikipedia.org/wiki/S2CID_(identifier) "S2CID (identifier)") [445002](https://api.semanticscholar.org/CorpusID:445002).
10.  **[^](https://en.wikipedia.org/wiki/DBSCAN#cite_ref-11 "Jump up")** Beer, Anna; Draganov, Andrew; Hohma, Ellen; Jahn, Philipp; Frey, Christian M.M.; Assent, Ira (6 August 2023). ["Connecting the Dots -- Density-Connectivity Distance unifies DBSCAN, k-Center and Spectral Clustering"](https://dl.acm.org/doi/pdf/10.1145/3580305.3599283). _Proceedings of the 29th ACM SIGKDD Conference on Knowledge Discovery and Data Mining_. ACM. pp. 80–92\. [doi](https://en.wikipedia.org/wiki/Doi_(identifier) "Doi (identifier)"):[10.1145/3580305.3599283](https://doi.org/10.1145%2F3580305.3599283). [ISBN](https://en.wikipedia.org/wiki/ISBN_(identifier) "ISBN (identifier)") [9798400701030](https://en.wikipedia.org/wiki/Special:BookSources/9798400701030 "Special:BookSources/9798400701030"). [S2CID](https://en.wikipedia.org/wiki/S2CID_(identifier) "S2CID (identifier)") [260499476](https://api.semanticscholar.org/CorpusID:260499476).
11.  **[^](https://en.wikipedia.org/wiki/DBSCAN#cite_ref-WIREs_12-0 "Jump up")** [Kriegel, Hans-Peter](https://en.wikipedia.org/wiki/Hans-Peter_Kriegel "Hans-Peter Kriegel"); Kröger, Peer; [Sander, Jörg](https://en.wikipedia.org/w/index.php?title=J%C3%B6rg_Sander&action=edit&redlink=1 "Jörg Sander (page does not exist)"); [Zimek, Arthur](https://en.wikipedia.org/wiki/Arthur_Zimek "Arthur Zimek") (2011). ["Density-based Clustering"](https://web.archive.org/web/20161117070919/http://wires.wiley.com/WileyCDA/WiresArticle/wisId-WIDM30.html). _WIREs Data Mining and Knowledge Discovery_. **1** (3): 231–240\. [doi](https://en.wikipedia.org/wiki/Doi_(identifier) "Doi (identifier)"):[10.1002/widm.30](https://doi.org/10.1002%2Fwidm.30). [S2CID](https://en.wikipedia.org/wiki/S2CID_(identifier) "S2CID (identifier)") [36920706](https://api.semanticscholar.org/CorpusID:36920706). Archived from [the original](http://wires.wiley.com/WileyCDA/WiresArticle/wisId-WIDM30.html) on 2016-11-17. Retrieved 2011-12-12.
12.  **[^](https://en.wikipedia.org/wiki/DBSCAN#cite_ref-13 "Jump up")** [Schubert, Erich](https://en.wikipedia.org/w/index.php?title=Erich_Schubert&action=edit&redlink=1 "Erich Schubert (page does not exist)"); Hess, Sibylle; [Morik, Katharina](https://en.wikipedia.org/wiki/Katharina_Morik "Katharina Morik") (2018). [_The Relationship of DBSCAN to Matrix Factorization and Spectral Clustering_](http://ceur-ws.org/Vol-2191/paper38.pdf) (PDF). Lernen, Wissen, Daten, Analysen (LWDA). pp. 330–334 – via CEUR-WS.org.
13.  **[^](https://en.wikipedia.org/wiki/DBSCAN#cite_ref-14 "Jump up")** [Sander, Jörg](https://en.wikipedia.org/w/index.php?title=J%C3%B6rg_Sander&action=edit&redlink=1 "Jörg Sander (page does not exist)") (1998). _Generalized Density-Based Clustering for Spatial Data Mining_. München: Herbert Utz Verlag. [ISBN](https://en.wikipedia.org/wiki/ISBN_(identifier) "ISBN (identifier)") [3-89675-469-6](https://en.wikipedia.org/wiki/Special:BookSources/3-89675-469-6 "Special:BookSources/3-89675-469-6").
14.  **[^](https://en.wikipedia.org/wiki/DBSCAN#cite_ref-15 "Jump up")** Campello, R. J. G. B.; Moulavi, D.; [Zimek, A.](https://en.wikipedia.org/wiki/Arthur_Zimek "Arthur Zimek"); [Sander, J.](https://en.wikipedia.org/w/index.php?title=J%C3%B6rg_Sander&action=edit&redlink=1 "Jörg Sander (page does not exist)") (2013). "A framework for semi-supervised and unsupervised optimal extraction of clusters from hierarchies". _Data Mining and Knowledge Discovery_. **27** (3): 344. [doi](https://en.wikipedia.org/wiki/Doi_(identifier) "Doi (identifier)"):[10.1007/s10618-013-0311-4](https://doi.org/10.1007%2Fs10618-013-0311-4). [S2CID](https://en.wikipedia.org/wiki/S2CID_(identifier) "S2CID (identifier)") [8144686](https://api.semanticscholar.org/CorpusID:8144686).
15.  **[^](https://en.wikipedia.org/wiki/DBSCAN#cite_ref-16 "Jump up")** [Kriegel, Hans-Peter](https://en.wikipedia.org/wiki/Hans-Peter_Kriegel "Hans-Peter Kriegel"); [Schubert, Erich](https://en.wikipedia.org/w/index.php?title=Erich_Schubert&action=edit&redlink=1 "Erich Schubert (page does not exist)"); [Zimek, Arthur](https://en.wikipedia.org/wiki/Arthur_Zimek "Arthur Zimek") (2016). "The (black) art of runtime evaluation: Are we comparing algorithms or implementations?". _Knowledge and Information Systems_. **52** (2): 341. [doi](https://en.wikipedia.org/wiki/Doi_(identifier) "Doi (identifier)"):[10.1007/s10115-016-1004-2](https://doi.org/10.1007%2Fs10115-016-1004-2). [ISSN](https://en.wikipedia.org/wiki/ISSN_(identifier) "ISSN (identifier)") [0219-1377](https://search.worldcat.org/issn/0219-1377). [S2CID](https://en.wikipedia.org/wiki/S2CID_(identifier) "S2CID (identifier)") [40772241](https://api.semanticscholar.org/CorpusID:40772241).
